name: Write Salam to log.txt

on:
  push:
  schedule:
    - cron: '0 7 * * *'  # كل يوم الساعة 7 صباحًا UTC
  workflow_dispatch:      # يمكن تشغيله يدويًا أيضًا

permissions:
  contents: write   # 👈 ضروري حتى يُسمح له بالدفع (push)

jobs:
  write-salam:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Write "السلام عليكم" to log.txt
        run: |
          echo "السلام عليكم ورحمة الله وبركاته 🌸 - $(date '+%Y-%m-%d %H:%M:%S')" >> log.txt

      - name: Commit and push changes
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add log.txt
          git commit -m "🤍 أُضيف سطر سلام جديد إلى log.txt" || echo "No changes to commit"
          git push
